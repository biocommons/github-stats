[project]
name = "github-stats"
version = "0.1.0"
description = "GitHub activity stats collector for biocommons"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]

[tool.uv]
managed = true
